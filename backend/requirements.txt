# ── Web framework ─────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6

# ── Validation ────────────────────────────────────
pydantic==2.9.2
pydantic-settings==2.5.2

# ── HTTP client ───────────────────────────────────
httpx==0.28.1

# ── Gemini AI (official Google SDK) ───────────────
google-genai==1.19.0

# ── Supabase (client only — schema in M3) ─────────
# NOTE: supabase 2.7.4 requires httpx<0.28 which conflicts with google-genai.
# Will be upgraded to a compatible supabase release in Milestone 3.
# supabase==2.7.4

# ── Dev / testing ─────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
